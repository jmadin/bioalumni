<div class="row">
  <div class="col-md-6 col-md-offset-3">
		<h1>New upload</h1>

		<%= render 'form' %>



    <% if @photo.alum %>
      <%= link_to 'Back', alum_path(@photo.alum) %>
    <% elsif params[:alum_id] %>
      <%= link_to 'Back', alum_path(params[:alum_id]) %>
    <% else %>
      <%= link_to 'Back', photos_path %>
    <% end %>

  </div>
</div>